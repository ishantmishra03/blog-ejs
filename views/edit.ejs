<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post | Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen text-white bg-zinc-900">
      <!-- Title  -->
      <div class="p-5">
        <form method="post" action="/update/<%= post._id %>">
          <textarea
            name="content"
            class="w-1/3 bg-zinc-800  rounded-md border border-white/10 text-white p-3"
            rows="3"
          ><%= post.content %></textarea>
          <button
            type="submit"
            class="w-40 mt-1 block px-3 py-2 rounded-md bg-yellow-500 text-black cursor-pointer hover:bg-blue-600 transition-all text-sm"
          >
            Update Post
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
